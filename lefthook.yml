pre-commit:
  parallel: true
  commands:
    lint-staged:
      run: pnpm lint-staged

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}

pre-push:
  parallel: true
  commands:
    type-check:
      run: pnpm type-check
    build:
      run: pnpm build-only
